services:
  db:
    image: mysql:9
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 123
      MYSQL_DATABASE: cardapio

    ports:
      - '3306:3306'

    volumes:
      - mysql.cardapio:/var/lib/mysql

volumes:
  mysql.cardapio:





